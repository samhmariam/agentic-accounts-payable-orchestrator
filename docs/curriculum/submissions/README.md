# Curriculum Submission Handoffs

Learner-authored tracked submission artifacts live here when a workflow or review contract needs them in Git.

`aegisap-lab rubric-check --day XX` writes both the local working artifact under `build/dayXX/` and the tracked handoff file under `docs/curriculum/submissions/dayXX/`.
