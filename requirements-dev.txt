# Compatibility export for development installs.
# The source of truth for dependencies is pyproject.toml + uv.lock.
# Evaluation / dev only - not installed in production containers
-r requirements.txt

dspy-ai==3.1.3
ragas==0.4.3
