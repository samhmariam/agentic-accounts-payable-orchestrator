AppTraces
| extend dependency_name = tostring(Properties["dependency_name"])
| where Message has "retry_" or tostring(Properties["event_name"]) has "retry_"
| summarize retries=count() by dependency_name
| order by retries desc
