# Day 00 Rosetta Stone - Bootstrap Contract

- `Portal truth`: subscription, resource group, region, deployment names, service endpoints, and the identity boundary that the chosen track should load.
- `Notebook proof`: inspect `.day0/<track>.json` and compare it to the intended Azure estate.
- `Repo fix`: repair the Day 0 state contract, then rerun `scripts/setup-env.sh` plus `scripts/verify_env.py`.
- `Durable owner`: provisioning scripts, bootstrap state file generation, and Day 0 documentation.
