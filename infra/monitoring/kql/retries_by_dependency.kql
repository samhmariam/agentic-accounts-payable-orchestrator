traces
| extend dependency_name = tostring(customDimensions["dependency_name"])
| where message has "retry_" or tostring(customDimensions["event_name"]) has "retry_"
| summarize retries=count() by dependency_name
| order by retries desc
