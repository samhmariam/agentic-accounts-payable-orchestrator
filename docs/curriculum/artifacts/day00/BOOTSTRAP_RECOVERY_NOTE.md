# Day 00 Bootstrap Recovery Note

Use this artifact to record:

- selected track (`core` or `full`)
- first failure signal from `setup-env.sh` or `verify_env.py`
- Azure truth used to validate the fix
- durable repo surface updated or re-provisioned
- final verification command and result
